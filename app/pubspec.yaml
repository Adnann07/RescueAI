name: disaster_voice_app
description: Disaster Management Voice AI App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_sound: ^9.2.13
  permission_handler: ^11.3.1
  http: ^1.2.1
  flutter_tts: 3.8.5
  path_provider: ^2.1.3
  geolocator: ^13.0.2
  shared_preferences: ^2.3.2
  cupertino_icons: ^1.0.6
  socket_io_client: ^2.0.2     # CrisisMap BD rescuer tracking

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true